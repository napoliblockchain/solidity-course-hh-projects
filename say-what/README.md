# SayWhat

```shell
npx hardhat run scripts/deploy.js --network <goerli|hardhat> # deploy

npx hardhat test # run tests

npx hardhat set-what --address <address> --what <what> --network <goerli|hardhat> # call setWhat(string)
```